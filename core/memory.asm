; --- core/memory.asm ---

; Функция: Выделить память
; Вход: RCX - размер в байтах
; Выход: RAX - указатель на выделенную память (или 0, если ошибка)
rs_alloc:
    ; Здесь должен быть вызов системного аллокатора твоего ядра
    ; Например, через прерывание, syscall или прямой call в ядро:
    ; call os_kmalloc 
    ret

; Функция: Освободить память
; Вход: RCX - указатель на блок
rs_free:
    ; call os_kfree
    ret